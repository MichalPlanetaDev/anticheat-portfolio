[package]
name = "ac-server"
version = "0.1.0"
edition = "2024"

[dependencies]
ac-protocol = { path = "../ac-protocol" }
ac-detectors = { path = "../ac-detectors" }
ac-telemetry = { path = "../ac-telemetry" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"